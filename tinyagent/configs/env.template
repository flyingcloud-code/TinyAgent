# TinyAgent 环境变量配置 (简化版)
# 复制此文件为 .env 到项目根目录，设置您的API密钥

# ========================================
# 必需配置: 只需设置一个API密钥即可启动
# ========================================

# 推荐: OpenRouter (最稳定，支持多种模型)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# 或者使用 OpenAI
#OPENAI_API_KEY=your-openai-api-key-here

# 或者使用 Azure OpenAI  
#AZURE_OPENAI_API_KEY=your-azure-api-key-here
#AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# ========================================
# 可选配置: 默认值已经很好，通常不需要修改
# ========================================

# 覆盖LLM提供商 (默认: openrouter)
#TINYAGENT_LLM_PROVIDER=openai

# 覆盖模型 (默认: deepseek/deepseek-chat-v3-0324)
#TINYAGENT_MODEL=gpt-4

# 覆盖日志级别 (默认: INFO)
#TINYAGENT_LOG_LEVEL=DEBUG

# 禁用智能模式 (默认: true)
#TINYAGENT_INTELLIGENT_MODE=false

# ========================================
# 零配置启动说明
# ========================================
# 1. 设置上面任一API密钥
# 2. 运行: python -m tinyagent "你的任务"  
# 3. 全部其他配置使用智能默认值，无需设置 